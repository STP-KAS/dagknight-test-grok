[package]
name = "kip2-confirmation-policy"
version = "0.1.0"
edition = "2021"
publish = false
license = "MIT"
description = "Standalone KIP-2 confirmation-policy types. Other files in tests/ are patches for rusty-kaspa dagknight."
autotests = false
autobenches = false
autoexamples = false

[lib]
name = "confirmation_policy"
path = "tests/confirmation_policy.rs"
