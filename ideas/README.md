# Ideas

Open `inbox.md` and append. One idea per bullet, dated.

Promoted ideas become docs (confirmation policy, L1 hooks, tests). Keep the original inbox bullet and add `(promoted → docs/…)` so the trail stays.
